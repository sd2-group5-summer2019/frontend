import React from 'react';


class Register extends React.Component{
    //Basic for now feel free to change add code to where ever
    // i just added this so it didn't give me an error when I checked to make sure
    // the routing was working correctly
    render(){
        return(
            <h1>Register Page</h1>
        )
    }
}

export default Register;